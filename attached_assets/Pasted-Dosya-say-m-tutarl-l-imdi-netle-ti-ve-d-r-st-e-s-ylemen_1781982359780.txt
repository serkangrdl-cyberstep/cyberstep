Dosya sayım tutarlılığı şimdi netleşti, ve dürüstçe söylemen iyi ("önceki tespit yanlıştı") — ama bu turun asıl önemli sonucu farklı: getClaudeAiFn evreni 3 dosyadan 20 dosyaya çıktı. Bu, "35 dosyalık plan"ın baştan beri eksik olduğu anlamına geliyor — sadece sayım hatası değil, gerçek kapsam neredeyse iki katına çıktı. Faz C'ye geçmeden önce bunu kapatmamız lazım:
1. Bu 20'lik liste gerçekten son mu? Tek seferlik, kesin bir repo taraması iste
Üç tur boyunca (3 dosya → 8 dosya → 20 dosya) liste büyüdü, her seferinde "araştırınca bulduk" diye. Bir dördüncü tur istemiyorum. Şunu net olarak sor: repo'da Anthropic API'ye giden HER çağrı yolu (hem anthropic.messages.create direct call hem getClaudeAiFn hem başka bir wrapper varsa o) için tek bir grep/AST taraması yap, kesin ve nihai sayıyı raporla. "20 dosya" şu anki en iyi tahmin mi, yoksa kapsamlı bir taramanın sonucu mu — bunu ayırt et.
2. İki dosya, XSS paterni nedeniyle Faz C sırasına değil, hemen şimdi kontrol edilmeli
Listede onboardingEmailSeries.ts ve teaserReportService.ts var — isimlerinden, leadTeaserEmail.ts ile aynı sınıfta görünüyorlar: sıfır insan gözetimiyle, otomatik olarak müşteri adayına giden e-posta/rapor içeriği. Az önce tam bu paternde (leadTeaserEmail.ts) gerçek bir XSS açığı bulup düzelttik. Bu iki dosyada AI çıktısı bir HTML template'e raw inject ediliyor mu, ediliyorsa escapeHtml() var mı — bunu Faz C'nin genel migrasyon sırasını beklemeden, şimdi, ayrı ve hızlı bir kontrol olarak yap. Bulgu olumluysa (zaten güvenliyse) bir cümlelik teyit yeterli.
3. leadScoringService.ts — iş modelinin merkezinde, ayrı önceliklendirilmeli
Bu dosya, 200 KOBİ'ye otomatik satış stratejisinin tam kalbi — hangi domain'in qualified lead sayılacağına karar veren mantık. İçinde bir AI çağrısı varsa, bu cve-content.json ile aynı önceliği hak ediyor: gerçek bir kalite golden set'i (doğru skorlanmış/yanlış skorlanmış örnekler). Faz C borç listesine "diğer 19 dosyayla aynı sırada" değil, cve-content.json'ın hemen yanında, üst sırada koy.
4. src/index.ts netliği — net evet/hayır istiyorum
"ai-tools-report görevi değil, ai-tool-monitor.ts'i import ediyor" cevabı kısmi. Net soru: src/index.ts'in kendisinde, doğrudan bir Anthropic API çağrısı (ne callModel ne ham anthropic.messages.create) var mı, yok mu? Yoksa bu dosyayı hem Faz A/B/C refactor listesinden hem de borç listesinden tamamen çıkar — sadece bir servisi import ediyor olması onu API-çağıran dosya yapmaz. Varsa, hangi satırda, hangi task etiketiyle Faz C'ye eklenir.
Onaylanan kısımlar: policy-generator.ts vs ciso/policyGenerator.ts ayrımı net, kabul. cve-content.json runner'ın Faz C başında geleceği kabul. Blog XSS kontrolü (DOMPurify.sanitize()) ve sosyal medya SVG ayrımı tatmin edici, kapandı.
Aksiyon: Faz C'ye başlamadan önce: (1) kesin/nihai çağrı-yeri sayımını tek taramayla doğrula, (2) onboardingEmailSeries.ts + teaserReportService.ts'i XSS paterni için hemen kontrol et, (3) leadScoringService.ts'i öncelikli golden-set adayı olarak işaretle, (4) src/index.ts için net evet/hayır cevabı ver. Bunlar tamamlanınca Faz C'ye onay veririm.