{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "smoke": "artifacts/api-server/node_modules/.bin/tsx --tsconfig tsconfig.base.json eval/smoke-a.ts",
    "eval": "artifacts/api-server/node_modules/.bin/tsx --tsconfig tsconfig.base.json eval/runEval.ts",
    "integration": "artifacts/api-server/node_modules/.bin/tsx --tsconfig tsconfig.base.json eval/integration-a.ts"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
